return {
    filetypes = { "conf" },
    cmd = { "hyprls" },
    settings = {
        hyprls = {
            preferIgnoreFile = false,
            ignore = { "hyprlock.conf", "hypridle.conf" }
        }
    }
}
